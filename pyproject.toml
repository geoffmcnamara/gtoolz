[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gtoolz"
version = "0.1.6a0"
dependencies = [
    "docopt",
    # (include any other dependencies your project requires)
]

[tool.setuptools]
py-modules = ["gtoolz"]

